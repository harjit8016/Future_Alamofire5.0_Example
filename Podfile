# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'CombineFuture' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for CombineFuture
    pod 'Alamofire', '~> 5.1'

  target 'CombineFutureTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'CombineFutureUITests' do
    # Pods for testing
  end

end
